/**
 * Git-related shared constants.
 *
 * Single source of truth for default values used across git operations.
 */

/** Default branch name used as fallback when no branch is configured. */
export const GIT_DEFAULT_BRANCH = "main";
